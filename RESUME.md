# 简历项目描述 — AI 旅游助手（travel-java-server）

> 三档：完整档（简历正文，约 350 字）、精简档（1-2 行，用于排版紧张时）、STAR 面试版（按问题展开）。
> 数据均来自 `docs/benchmark.md` 与本仓库真实代码，可直接对照源码答辩。

---

## 一、完整档（简历正文）

**AI 智能旅游助手 · 后端独立开发**　　Spring Boot / OkHttp / Redis / MySQL / Milvus

不使用 Spring AI、LangChain 等 AI 框架，基于 OkHttp 直连大模型 API 手写完整 AI 应用后端链路，33 个类约 2200 行核心代码。实现 SSE 流式对话（打字机式回复）、Redis 滑动窗口多轮记忆、Function Calling 三工具调用、Milvus 向量 RAG 检索增强、用户体系（JWT 无状态免，改用 Redis 不透明 token 换取登出即时失效）、JMeter 压测定位瓶颈。

- **SSE 流式 + 有界线程池**：手写 SSE 协议解析（`data:`/`[DONE]`/思考标签剥离），线程池配 8 核 32 队列 + CallerRunsPolicy 背压，避免 LLM 长耗时请求拖垮 Tomcat 线程池
- **会话记忆**：Redis List 做 LTRIM 滑动窗口 + EXPIRE 30 分钟空闲过期，20 条上限控制 token 成本；空回答不落库防毒丸，过滤孤儿 assistant 消息头防上游 400
- **Function Calling**：JSON-Schema 注册城市预算/高德天气/高德 POI 三工具，流式 `tool_calls` 按 index 累积重组；针对上游"拒绝连续 tool 消息"的缺陷，把 N 个并行调用拆成 N 对 assistant→tool 串行回传
- **RAG 检索增强**：bge-m3 1024 维向量化，按 markdown 小节切块 + 城市前缀注入，Cosine 检索；用"针头测试"埋虚构事实验证召回有效性，按实测分数分布（相关 0.78+ / 无关 0.49~0.56）校准阈值 0.6
- **性能验证**：JMeter 三轮压测，5 并发全接口 0 错误，P95 定位到模型推理侧（顶 OkHttp 120s 读超时）而非应用代码，据此确定优化方向为流式化推荐接口而非加机器

---

## 二、精简档（1~2 行）

- **AI 旅游助手后端**：不依赖 Spring AI，OkHttp 直连模型 API 手写 SSE 流式对话 + Redis 滑动窗口记忆 + Function Calling + Milvus RAG 全链路；JMeter 压测 5 并发 0 错误，定位瓶颈在模型推理侧。
- **AI 应用后端**：手写大模型对话协议/上下文工程/检索增强/工具调用，含工具并行调用串行化、RAG 阈值实测校准、会话记忆毒丸防护等工程细节；2200 行核心代码，JMeter 三轮 0 错误。

---

## 三、STAR 面试版（按高频问题展开）

### Q1：为什么不直接用 Spring AI / LangChain？
**S**：要做一个 AI 旅游助手，行业默认方案是套框架。
**T**：想真正搞懂 AI 后端的链路细节，而不是把协议层黑盒化。
**A**：OkHttp 直连 SiliconFlow 的 OpenAI 兼容接口，手写 JSON-Schema 工具注册、流式 `tool_calls` 按 index 重组、SSE 协议解析、会话记忆管理。框架的价值在于"能跑"，但我需要的是"知道它为什么这么跑"。
**R**：这个选择直接暴露了框架掩盖的三个真实问题——上游拒绝连续 tool 消息、工具名幻觉、空回答污染记忆，这三个只有自己掌控协议层才能定位和修掉。

### Q2：Function Calling 的坑怎么解决的？（技术亮点）
**S**：模型一轮并行发起 2 个工具调用时，上游返回 400 报错。
**T**：让工具调用在并发场景稳定可用。
**A**：先用裸 API 实验复现，定位到上游校验器拒绝"连续 tool 消息"（报错 `after tool message, next must be user or assistant`）。修法是把 N 个并行调用拆成 N 对 `assistant(单个 call) → tool(对应结果)` 串行回传。
**R**：上游接受且模型能综合多结果作答。另外把 `parallel_tool_calls:false` 排除掉——实测上游只"接受"这个字段但不真约束模型行为，串行化才是无论模型怎么发都成立的兜底。

### Q3：RAG 的相似度阈值为什么是 0.6？
**S**：向量检索永远返回最近的 K 条，库里没相关内容也会强行召回。
**T**：让"无相关知识"和"有相关知识"有明确边界，避免模型编造。
**A**：先写 `/api/knowledge/search` 纯检索接口（不过 LLM）看真实分数分布，发现 bge-m3 分数天然分两群：相关命中 0.78+，无关问题 0.49~0.56。取两群中间的 0.6 一刀切开。
**R**：库外问题召回 0 条，模型如实说"暂不支持"；语料里埋的 3 处虚构针头事实全部答对。教训是相似度阈值不能抄博客，必须用自家 embedding 模型实测分数分布来定。

### Q4：会话记忆怎么防止 token 膨胀和脏数据？
**A**：四个防护点——①LTRIM 只留最近 20 条；②EXPIRE 30 分钟空闲自动过期；③空回答不写入记忆（否则下一轮模型看到空 assistant 会学坏）；④裁窗后过滤孤儿 assistant 消息头，保证对话必然以 user 开头。
**R**：max-messages 特意设成偶数——奇数时 LTRIM 会裁出以 assistant 开头的孤儿消息，上游直接报 400。这类问题只有端到端真跑才测得出。

### Q5：压测结果说明了什么？
**A**：JMeter Headless 三轮（改造前/B 阶段后/全功能版），5 并发下全接口 0 错误，行程推荐均值 82.8s、P95 120.1s。
**R**：结论是 P95 精确顶在 OkHttp 120s 读超时上，说明瓶颈在模型生成 2000+ token 的固有耗时，不在应用代码。所以应用侧的优化方向不是加机器，而是把推荐接口也流式化（已列为后续计划）。这是 LLM 应用的典型特征：耗时与"是否触发工具"强相关，与 QPS 相关性低。

### Q6：怎么验证 RAG 真的有效而不是模型自己在编？
**A**："针头测试"法：在 5 城的 20 篇语料里故意埋 3 处模型凭常识不可能知道的虚构事实（比如虚构的节日、虚构的交通规则），再问相关问题。
**R**：答对 = 检索链路真的通；再配一组库外问题作对照组，应召回 0 条且不编造。两个条件都满足才算 RAG 有效，否则只是"模型说得比较像"。

### Q7：密钥和部署怎么处理？
**A**：所有 yml 零真实密钥，业务密钥全部 `${ENV_VAR}` 占位符注入（本地 application-dev.yml 已被 gitignore，服务器走 docker `env_file`），另提交 `application-dev-example.yml` 模板。密钥缺失时启动期直接报 `Could not resolve placeholder` 崩溃。
**R**：这是刻意的 fail-fast——好过带病上线、调模型时才炸。配合高德 key 配 IP 白名单，泄露也无法被盗用。

---

## 四、可主动抛出的已知边界（面试加分项）

1. 推荐接口仍是同步返回完整 JSON，未流式化——大模型生成 2000+ token 时前端需干等，这是已识别的优化方向
2. 未做接口级限流，高并发下会直接打到上游配额
3. 压测为同机施压（JMeter 与 Spring Boot 同机），绝对值偏保守，仅用于同环境纵向对比

---

## 五、技术栈关键词（简历技能栏可复用）

Java 17 · Spring Boot 4.1 · MySQL 8（JdbcTemplate）· Redis 8 · Milvus 2.5（向量库）· SiliconFlow GLM-4.5V（对话）+ BAAI/bge-m3（向量化）· OkHttp 4 · JMeter 5.6 · Docker Compose · Vue3 + Vant（配套 H5）

> 一句话定位：能脱离 AI 框架、自己把手写大模型后端链路从头搭起来并压测验证的后端能力。
