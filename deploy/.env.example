# 复制为 .env（已被根 .gitignore 屏蔽，绝不入库）：cp .env.example .env
# docker compose 自动读取同目录 .env 做变量插值
DB_PASSWORD=改成你自己的强密码（至少8位，勿用示例值）
LLM_API_KEY=sk-你的SiliconFlow密钥
AMAP_KEY=你的高德Web服务Key
# Redis/Milvus 所在机器的 IP（= VM 的 IP）。换网络环境 VM 换 IP 后，取消注释改成新 IP
# REDIS_HOST=192.168.11.194
